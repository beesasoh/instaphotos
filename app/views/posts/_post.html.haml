%li
  .card
    .user-info
      = link_to post.user.username, profile_path(post.user.username)

    .post-image
      = image_tag post.image.url(:medium)

    .post-extras
      .post-caption
        = post.caption